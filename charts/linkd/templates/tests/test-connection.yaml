apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linkd.fullname" . }}-test-connection"
  labels:
{{ include "linkd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "linkd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
