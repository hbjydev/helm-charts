apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkd.fullname" . }}
  labels:
    {{- include "linkd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linkd.selectorLabels" . | nindent 4 }}
